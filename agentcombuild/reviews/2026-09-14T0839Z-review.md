# TIMESTAMPED REVIEW — 2026-09-14T08:39:56Z

Author: build session (self-review, evidence-linked). Base: main @ push
after `59f2cdc`. Method: every claim below names the artifact that would
prove it wrong. No prose verdicts without pointers.

## What is real (would survive a hostile re-run)

- Proof stack: gates fail closed, grants fail closed, receipts verify,
  tamper fails, forgery refused — 100+ tests green across 10 suites,
  reproducible via `bash agentcombuild/GO.sh` (16/16 at review time).
- Real QP boundary: ids minted, grants verified, transitions settled and
  re-settled by `~/qp` code, not ours. Atask sandbox cycles real.
- Ordering laws enforced structurally: settlement unreachable without TRUE
  readback (proven by raising-monkeypatch test, not by assertion); contract
  changes on all 7 WHAT-fields, stable on all 5 HOW-fields.
- Dual custody real: 15 trajectories banked as full+ATIF+compact with
  fidelity gates; 3 session logs with every command and output tail.
- Wire compat real: official MCP client handshake + SDK constructors green
  under venv, keyless.

## What is theatre (looks like progress, isn't)

1. **Simulated lanes called tournaments.** E4's "lanes" are fixture
   functions, not builders. The tournament machinery is real; the
   contestants are not. Until distinct builders disagree on a real task,
   tournament wins mean nothing.
2. **Trajectory bank of simulations.** 15 banked trajectories, zero live
   ones. The bank, fidelity gate, and memory compiler are proven
   *mechanisms* with no *experience* that matters yet.
3. **Memory compiler over one idea.** Reduction ratios computed on toy
   inputs. The "20 visionary ideas by run 4" story is aspirational, not
   measured — current bank holds a handful.
4. **Product with no user.** `dist/plugins/agentcom-uk/` builds, lints
   (minus world gates), and bundles — installable by nobody, installed
   nowhere. The 13 TRUE leaves are all self-administered exams.
5. **VERDICT.json files as artifacts.** Reviews write them, nobody reads
   them except the next review run. Fine as machine state; not evidence
   of anything external.
6. **CI workflows unobserved.** Two workflow files committed, zero observed
   runs on GitHub. A CI config that never executed is a wish, and the
   "independent second machine" claim is currently void.
7. **Stubs counted as roadmap.** ab4–10, domainpacks, voice, OPA/WASM,
   scheduler richness — directory names, not capabilities. Harmless only
   while nobody mistakes them for progress.

## High-impact next steps (ranked by information-per-effort)

1. **Observe CI run once.** Push any commit, watch `agentcom-contract` go
   green on GitHub's machine. Closes the second-machine gap. Effort: ~0
   (already committed). Unblocks: trust in every later claim.
2. **One API key → F2a live read.** Single cheapest reality contact in the
   whole plan. Unblocks: provider telemetry, live legs, E6 rehearsal.
3. **One real permission → E6.** The entire architecture converges on one
   bounded consequential action with readback. Everything else is preamble.
4. **E4 with real lanes.** Same frozen contract, two genuinely different
   builders (e.g., direct vs cg-executor vs scripted-policy). First
   tournament whose winner means something.
5. **Run-2-beats-run-1.** The terminal test. Currently at zero attempts.

## Explicitly not next (theatre watchlist)

Dashboards, plugin polish, scheduler richness, WASM/Rego, second campaign,
voice, OPA policies, embeddings, more schemas, more stubs, more reviews
of reviews. Any commit in these areas before item 3 above is motion.

## The one question

> What is the cheapest action that could prove a current belief FALSE?

Today: run the F2a live read. If the provider shape differs from the
fixture, half the sim-side contracts need rewriting — that failure would
be worth more than the last 17 commits combined.
