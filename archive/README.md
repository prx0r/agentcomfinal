# archive/ — frozen experimental history (E0 declaration)

ab1–ab3 (in `agentcombuild/`) discovered compiler/gate shape,
authority/signature issues, and Actuality/A-log shape. They are DONE as
experiments: tested, reviewed, referenced — never extended. Their surviving
primitives live promoted in `core/ autobuild/ trajectory/` with provenance
headers. Future experiments run as Seed0 lanes (`experiments/policies/`),
not as `autobuild4+` forks.
