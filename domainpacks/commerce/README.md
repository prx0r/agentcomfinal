# domainpacks/commerce — fragmented-commerce targets (BreadUp, AgentUgly)

Live product code stays in its own repos/checkouts (`~/breadup`, `~/drop`,
`~/pogtown`). This pack will hold ONLY the compiled Actuality contracts +
validator refs for commerce capabilities (listing created, order filled,
payout settled + readback). Nothing here executes.
