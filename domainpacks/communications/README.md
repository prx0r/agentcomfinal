# domainpacks/communications — Cmail/voice targets

Live code stays in `~/cmail`, `~/voiceagent`. This pack will hold ONLY
compiled contracts for comms capabilities (message delivered + provider
readback, call connected + transcript). The agentcom-uk lead-reply leaf
set (`autobuild/compiler/actuality_compile.py`) is the first resident.
