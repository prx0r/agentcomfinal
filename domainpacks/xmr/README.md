# domainpacks/xmr — Monero commerce targets

Live code stays in `packages/xmrbot-private-procurement-v0.4/` (canonical)
and its siblings (archive). This pack will hold ONLY compiled contracts
for XMR capabilities (escrow funded, multisig released + chain readback).
No keys, no wallets, no spend paths in this tree — ever.
