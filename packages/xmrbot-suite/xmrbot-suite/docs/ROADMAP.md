# Roadmap

## Implemented in this archive

Core site, terminal, calculators, CPU pages, deterministic knowledge/resource router, API, MCP, local daemon, monerod adapter, xmr.computer bridge, content event objects, tests and validation.

## Adapter work intentionally left external

- live market-price feed with multi-source consensus
- real XMRig benchmark execution/parser
- signed binary installer/verifier
- P2Pool lifecycle manager
- Tor SOCKS routing implementation
- Qubic live quote/execution adapter
- external CPU-market adapters
- real wallet RPC execution with hardware-backed approval
- production TimescaleDB deployment
- content generation/publishing integrations
- crawler for hardware sold prices and benchmark observations

These are isolated interfaces rather than TODOs embedded in core logic.
